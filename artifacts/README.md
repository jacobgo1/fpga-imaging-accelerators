# Artifacts

Deliverables from successful runs, per `<kernel>/<run>/`: `ip/` (exported HLS IP
ZIP), `checkpoints/` (`.dcp`), `bitstream/` (`.bit`, `.ltx`), `hardware/`
(`.xsa`), plus the run's `manifest.json`. A failed run publishes nothing here.
Git-ignored.
