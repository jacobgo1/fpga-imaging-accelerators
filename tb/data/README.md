# Test data

Small, deterministic test vectors. For HLS simulation, add the data file to the
kernel's `testbench` list in `config/project.json` so the tool copies it into the
simulation directory, and open it by relative filename in the testbench.
