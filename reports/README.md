# Reports

Logs and reports collected after every run, passed or failed, per
`<kernel>/<run>/`, with the `manifest.json` that ties them to the inputs.
Raw HLS reports and waveforms stay in the matching `build/` directory.
Git-ignored — write conclusions worth keeping in `docs/experiments/`.
